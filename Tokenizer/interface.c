//Adrian Gomez Rodriguez
//CS3436
//Lab T-Th from 7:30 to 8:50
//TA: Daniel Cervantes

#include <stdio.h>
#include "tokenizer.h"
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
/*
int main(void){
  //milestone one
  int x;
  x = getchar();
  while (x != EOF){
    printf("$");
    putchar(x);
    x= getchar();
    
    //milestone two
    char c[] = "The cake is good";
    printf("The string length is %d \n", string_length(c));
    printf("The index is a valic character %c \n", is_valid_character(c));
    printf("The last index of the word is %d \n", find_word_end(c));
    printf("The first index of the word is %d \n", find_word_start(c));
    printf("The number of words is %d \n", count_words(c));
    print_tokens(c);
    return 0;

    
  }
}
*/
